import { ZoomImgDirective } from './zoom-img.directive';

describe('ZoomImgDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomImgDirective();
    expect(directive).toBeTruthy();
  });
});
